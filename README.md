# elm-mapboxgl-example

An example of how mapboxgl can be integrated into Elm, using ports.

To build `main.js`:

```
elm-make src/Main.elm --output build/main.js
```


You can then open `index.html` in your browser.
